# periodic-ive

The key files for the paper submission are `simple_sensitivity/toy_problem.jl` which runs the numerical study on the toy environment, and `chesapeake_bay/transect_opt_sim.ipynb`, which runs the simulation study with data from the Chesapeake Bay.
